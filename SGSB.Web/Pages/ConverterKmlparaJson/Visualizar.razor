
@page "/TipoEdificacao/Visualizar"
@page "/TipoEdificacao/Visualizar/{id}"

@inject TipoEdificacaoService TipoEdificacaoService
@inject NavigationManager NavigationManager
@inject Microsoft.JSInterop.IJSRuntime JSRuntime;
@using Model

@using BlazorTable
@using SGSB.Web.Data


<h3>Visualizar</h3>


<EditForm Model="@TipoEdificacaoModel" >
    <DataAnnotationsValidator />
    <ValidationSummary />
    <div class="row">
        <div class="col-md-8">
            <div class="form-group">
                <label for="nome" class="control-label">Nome da Tipo de Material de Barragem </label>
                <InputText @bind-Value="@TipoEdificacaoModel.Nome" type="Text" class="form-control" />
                <ValidationMessage For="@(()=> TipoEdificacaoModel.Nome)" />
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-md-4">
            <div class="form-group">
       
                <input type="button" class="btn" @onclick="@Cancel" value="Voltar" />
            </div>
        </div>
    </div>
</EditForm>

@code {
    private TipoEdificacao TipoEdificacaoModel = new TipoEdificacao();

    [Parameter]
    public string Id { get; set; }
    protected override async Task OnInitializedAsync()
    {


       await Ver(Id);
    }

    private async Task ChangeHandler(ChangeEventArgs args)
    {

    }

    public async Task Ver(string Id)
    {
        TipoEdificacaoModel =  await TipoEdificacaoService.GetTipoEdificacaoId(int.Parse(Id));
    }



  
    void Cancel()
    {
        NavigationManager.NavigateTo("TipoEdificacao/index");
    }


}
